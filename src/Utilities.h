#pragma once

#include <string>
#include <vector>

typedef long long ll;
typedef std::pair<int, int> pii;
typedef std::vector<int> vi;
typedef std::vector<pii> vpi;

float sigmoid(float x);

void eraseFromVector(std::string word, std::vector<std::string> &v);